# Puerto del servidor
PORT=4300

# Base de datos MySQL (redsocial) - OBLIGATORIO para persistir usuarios
# Copia este archivo a .env y configura tus credenciales
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=tu_password_mysql
DB_NAME=redsocial

# JWT (opcional)
# JWT_SECRET=lumina-secret-dev

# Microservicios (opcional - para arquitectura integrada)
# MICROSERVICIOS_URL=http://localhost:4200
