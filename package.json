{
  "name": "red-social-aprendizaje-colaborativo-inteligente",
  "version": "1.0.0",
  "description": "Plataforma donde los estudiantes co-crean conocimiento, la comunidad valida calidad, y un sistema inteligente organiza, recomienda y resume automáticamente",
  "scripts": {
    "install:all": "npm run install:backend && npm run install:frontend",
    "install:backend": "npm run install:backend:from-zero",
    "install:frontend": "cd frontend && npm install",
    "dev": "concurrently -n backend,frontend -c cyan,red \"npm run dev:backend:from-zero\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev:backend:from-zero",
    "db:init:redsocial": "mysql -u root -p < database/schemas/redsocial_FINAL.sql",
    "db:seed:redsocial": "mysql -u root -p < database/seeds/redsocial_seed.sql",
    "db:check": "npm run db:check --prefix backend/foro-estudiantes",
    "install:backend:from-zero": "npm run install:all --prefix backend",
    "dev:backend:from-zero": "npm run dev --prefix backend"
  },
  "keywords": [
    "red-social",
    "aprendizaje-colaborativo",
    "educación",
    "inteligencia-artificial",
    "microservicios",
    "react",
    "nodejs"
  ],
  "author": "Tu Nombre",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
